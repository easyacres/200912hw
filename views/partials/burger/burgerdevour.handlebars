<li>
	{{name}}

	<button class="devour-burger" data-id="{{id}}" data-newBurger="{{newBurger}}">
		{{#if devour}}{{else}}Name your Burger!{{/if}}
	</button>

	<button class="devour-burger" data-id="{{this.id}}">DEVOUR!</button>
</li>
